// Given K sorted lists of up to N elements in each list, return a sorted iterator over all the items.

/*

interface SortedIterator<Int> {
      constructor(List<List<Int>> input);
      boolean hasNext();
      Int next();
}

tuple()
[2,1,2]

1: [X, x, 5, 8, 9]
2: [X, 7, 7,10]
3: [X, X, 8]

YourIterator i; ...
i.next() // 0
i.hasNext() // true
i.next() // 0
i.next() // 1

...

*/

1234,
2345,
34

  List<Integer> store;  1,2,3
  List<List<Integer>> input;
  int lastChecked;

  public className(List<List<Int>> input){
    store = new ArrayList<>();
    for(List e: input){
      store.add(0);
    }
    this.input = input;

    lastChecked = 0
}

  public Int next(){
    for(int i = 0; i < input.size(); i++){
      if(lastChecked >= input.get(i).get(store.get(i))){

    }

  }

//- no elements
// - end of list
- duplicate in differnt arrays

Welcome to Facebook!

This is just a simple shared plaintext pad, with no execution capabilities.

When you know what language you would like to use for your interview,
simply choose it from the dropdown in the top bar.

Enjoy your interview!
